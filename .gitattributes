# Normalize text to LF in the repo (avoids CRLF churn from Windows editors).
* text=auto eol=lf

# Binary assets: never diff or convert line endings.
*.webp binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.pdf  binary
